# static-script

> This is an attempt to make a statically typed language

This project will experiment on if how a program can run if C like mechanics were used on a language's virtual machine.

This is going to be a 16 register virtual machine with a stack.
